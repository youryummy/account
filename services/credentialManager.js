export function login(req, res) {
    res.send({
        message: 'This is the mockup controller for login'
    });
}

export function register(req, res) {
    res.send({
        message: 'This is the mockup controller for addAccount'
    });
}

